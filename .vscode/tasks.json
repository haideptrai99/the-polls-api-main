{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run server fastapi",
            "type": "shell",
            "command": "${workspaceFolder}\\.venv\\Scripts\\activate && uvicorn main:app --reload",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "options": {
                "shell": {
                    "executable": "cmd.exe",
                    "args": ["/d", "/c"]
                }
            }
        }
    ]
}
